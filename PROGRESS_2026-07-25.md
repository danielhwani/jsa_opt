# GTX 1650 (4GB) 경량화 이식 — 진행 기록 (2026-07-25)

## 이전까지 완료된 작업
1. `Dockerfile`, `codes/config.py`, `scripts/generate_dataset.sh`, `run_docker.sh` 수정
   (`patch_size=128`, `max_epoch=100` 등). 원본은 `*.bak` 백업으로 보존.
2. 학습 파이프라인(`train.sh`) 100 epoch까지 OOM 없이 완주.
3. `eval.py`를 타일 기반(오버랩 블렌딩) 방식으로 수정 — 512x512 평가도 OOM 없이 성공.
4. `Dockerfile`에서 TensorRT 재활성화.

## 오늘 세션: TensorRT 벤치마크 재시도

### 배경
`benchmark_original_jsa_pth_vs_trt.sh`를 128x128 + best checkpoint + workspace 2GB로
실행하던 중 시스템 전체가 멈춰 강제 재부팅한 상태로 세션 시작.

### 1. 사전 점검 (문제 없음 확인)
- `nvidia-smi` 정상, GPU idle (데스크톱 프로세스만 195MiB 사용)
- `docker ps -a`에 좀비 컨테이너 없음 (무관한 `gitea` 컨테이너만 존재)
- `git status`로 이전 수정 파일(Dockerfile, config.py 등) 및 `.bak` 백업 모두 그대로 남아있음 확인
- 호스트 리소스 확인: RAM 7.7GB(가용 5.2GB), **스왑 0B** — 리스크 요인으로 식별

### 2. 안전장치를 갖춘 재시도
지난번 프리즈가 스왑 없는 상태에서 호스트 RAM 고갈로 인한 것일 가능성을 고려해
다음 안전장치를 추가:
- `docker run --memory=6g --memory-swap=6g` (호스트 RAM 상한, 컨테이너만 OOM-kill되고
  데스크톱 세션은 보존되도록)
- `--shm-size=2g` (기존 8G에서 축소)
- `HEIGHT=128 WIDTH=128 WORKSPACE_GB=1` (요청대로 workspace를 2GB→1GB로 축소)
- 로그 파일 tail(15초 간격) + 2분 무출력 시 타임아웃/강제종료하는 워치독 스크립트

### 3. 1차 시도 — 오탐(false positive)으로 컨테이너 강제종료
- `[1/2] Compile original JSA → torch2trt` 단계 진입 후 stdout이 멈춘 것처럼 보여
  워치독이 2분 무출력 판정으로 컨테이너를 강제 종료 (exit=137)
- 그러나 `nvidia-smi`/`docker stats`로 확인한 결과 GPU 17%, CPU 50%, 메모리 3.1GB로
  **실제로는 정상 진행 중**이었음
- **원인**: `docker run`이 TTY 없이 실행되어 컨테이너 내부 Python이 stdout을
  완전 버퍼링(full-buffering) 모드로 사용 — 프로세스는 살아있었지만 print 출력이
  버퍼에 갇혀 로그에 반영되지 않았던 것
- 시스템 자체는 전혀 멈추지 않음 (메모리 상한 덕분에 컨테이너만 정리되고 GPU/데스크톱 정상 복귀)

### 4. 2차 시도 — 성공
- `-e PYTHONUNBUFFERED=1` 추가하여 재시도
- 로그가 실시간으로 정상 출력되기 시작
- **JSA 원본 모델 → torch2trt 변환 성공** (지난번 시스템을 멈추게 했던 바로 그 단계)

### 벤치마크 결과 (JSA 원본, 128x128, FP16, workspace 1GB)

| 항목 | 평균 (ms) |
|---|---|
| PyTorch 원본 | 73.14 |
| PyTorch (export patch 적용) | 73.16 |
| torch2trt (FP16) | 35.78 |

- **속도 향상: 약 2.04배**
- 정확도 오차: TensorRT vs 원본 max abs diff = 0.00112 (허용 범위 내 미미한 수준)

생성된 산출물 (`benchmark_results/engine/`):
- `jsa_torch2trt_exportpatch_fp16_1x3x128x128.engine` (124MB)
- `jsa_torch2trt_exportpatch_fp16_1x3x128x128.torch2trt.pth` (182MB)
- `jsa_torch2trt_exportpatch_fp16_1x3x128x128.json` (타이밍/정확도 상세)

### JSA+Conv (2단계) — 예상된 실패
`config_cnn` 기준 conv 모델은 학습된 체크포인트가 없어 (`jsaCNN_classroom_overfit`
디렉터리 없음) `FileNotFoundError`로 종료. 실제 결함이 아니라 아직 conv 모델을
학습시키지 않은 상태이기 때문.

## 결론 / 재발 방지 포인트
- 지난번 프리즈의 근본 원인은 명확히 재현되지 않았으나(호스트 메모리 무제한 + 버퍼링으로
  인한 오판 가능성), 이번엔 **메모리 상한 + PYTHONUNBUFFERED=1** 조합으로 안전하게
  동일 단계를 통과함
- TensorRT 빌더(tactic profiling)는 실제로 진행 중이어도 수십~백여 초간 stdout이
  조용할 수 있음 — 무출력만으로 행(hang) 판정하지 말고 `nvidia-smi`/`docker stats`로
  GPU/CPU 활동을 함께 확인해야 함
- 앞으로 이런 류의 스크립트를 docker에서 돌릴 때는 항상 `-e PYTHONUNBUFFERED=1`과
  호스트 메모리 상한을 기본으로 넣는 것을 권장

## 다음 할 일
- `conv_train.py`로 JSA+Conv 모델 학습 → 체크포인트 생성
- 동일한 안전장치(workspace 1GB, memory 상한, PYTHONUNBUFFERED=1)로 conv 모델
  TensorRT 벤치마크 재시도
